---
const {
  class: className,
  id: id,
  isHeroParagraph,
  paragraphText,
  isCardParagraph,
  isCommentCardParagraph,
} = Astro.props
---

{
  isHeroParagraph ? (
    <p class="mt-6 mb-8 text-center text-lg text-white md:text-xl">
      {paragraphText}
    </p>
  ) : isCardParagraph ? (
    <p class="mb-8 line-clamp-4">{paragraphText}</p>
  ) : isCommentCardParagraph ? (
    <p>{paragraphText}</p>
  ) : (
    <p class={className} />
  )
}
