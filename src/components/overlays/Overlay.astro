---
const { class: className, id, isSingleViewOverlay } = Astro.props
---

{
  isSingleViewOverlay ? (
    <article class="h-full w-full absolute bg-black/50 z-[1000]" />
  ) : (
    <article class="h-screen w-screen absolute bg-black/65 z-[1000]" />
  )
}
