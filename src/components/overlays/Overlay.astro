---
const { class: className, id, variants } = Astro.props

const currentPath = Astro.url.pathname
const isHome = "/"

const baseOverlayClasses = `
  h-full w-full absolute bg-black/50
  ${currentPath !== isHome ? "rounded-md" : ""}
`
---

<article class={baseOverlayClasses}>
  <slot />
</article>
